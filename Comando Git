git config --global http.proxy ( 10.20.4.15:3128 )

git clone < url >

git status

git add .

git commit -m "mensaje"
git commit -am "mensaje"

git pull origin master

git push origin master ( Nombre de usuario ) 

git config --global user.email " "
git config --global user.email " " 
